applications:
    - name: cw-client
      path: ./dist
      buildpack: https://github.com/cloudfoundry/staticfile-buildpack.git
      random-route: false
      memory: 512M
      env:
          FORCE_HTTPS: true
